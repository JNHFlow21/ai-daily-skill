# Postmortem Reports

This directory stores AI-generated postmortems for fix commits and release analysis.

- Files named like `YYYYMMDD_<sha>_<slug>.md` are per-fix postmortems.
- Files named like `precheck_*.json` are pre-release trigger checks.
